#include "main.h"
#include <stdio.h>

int safe_close(int);

/**
 * main - execute other function
 * @argc: the number of passed arguments
 * @argv: the pointers to array of arguments
 *
 * Return: an integer
 */
int main(int argc, char *argv[])
{
	char buffer[1024
